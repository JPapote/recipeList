import { Component} from '@angular/core';
 

@Component({
    selector:'header-root',
    templateUrl: './header.component.html'
})
export class Header{
    // @Output() featureSelect = new EventEmitter<string>();

    // onSelect(feacture: string) {
    //     this.featureSelect.emit(feacture);
    // }
}